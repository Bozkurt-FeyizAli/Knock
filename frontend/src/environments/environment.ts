export const environment = {
  production: false,
  apiUrl: 'https://feyizalibozkurt.dev/api' // Adjust to the actual port your .NET backend runs on
  // apiUrl: 'http://localhost:5264/api'  if work in local machine
};
